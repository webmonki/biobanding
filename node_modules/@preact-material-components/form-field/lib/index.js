import { MaterialComponent } from '@preact-material-components/base/lib/MaterialComponent';
import { MDCFormField } from '@material/form-field';
import { h } from 'preact';
export class FormField extends MaterialComponent {
    constructor() {
        super(...arguments);
        this.componentName = 'form-field';
        this.mdcProps = ['align-end'];
    }
    componentDidMount() {
        if (this.control) {
            this.MDComponent = new MDCFormField(this.control);
        }
    }
    componentDidUpdate(props) {
        if (props.input !== this.props.input) {
            this.MDComponent.input = this.props.input;
        }
    }
    componentWillUnmount() {
        super.componentWillUnmount();
        if (this.MDComponent) {
            this.MDComponent.destroy();
        }
    }
    materialDom(props) {
        return (h("div", Object.assign({}, props, { ref: this.setControlRef }), this.props.children));
    }
}
//# sourceMappingURL=index.js.map